# Simple Bitcoin Wallet

This is a toy Bitcoin wallet created using the `bitcoinlib` library in Python. It provides basic functionalities like generating a new address, checking the balance, sending/receiving funds, and displaying the transaction history.

## Usage

```bash
python3 main.py
```
